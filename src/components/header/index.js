import React from 'react';
import Button from '@mui/material/Button';
import '../../MuiClassNameSetup'

const Header = () => {
  return (
    <footer>
      <Button variant="contained">Example remote</Button>
      Remote - header
    </footer>
  );
}

export default Header;